name: CI Serviciudad

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    name: Build, test y cobertura (JaCoCo)
    runs-on: ubuntu-latest

    strategy:
      matrix:
        java: [ '21', '25' ]

    defaults:
      run:
        working-directory: serviciudad-Backend

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ matrix.java }}
          cache: maven

      - name: Build + tests + verify coverage (mvn verify)
        run: mvn -B -ntp clean verify

      - name: Publicar reporte JaCoCo
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report-java-${{ matrix.java }}
          path: target/site/jacoco
          if-no-files-found: ignore

  canary-deploy:
    name: Canary deploy (placeholder)
    needs: build-and-test
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: serviciudad-Backend

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      # 👇 Aquí es donde David debe integrar los scripts que mandó el profe
      - name: Build Docker image (ejemplo)
        run: |
          echo "TODO: construir imagen Docker de serviciudad"
          # docker build -t serviciudad-cali:${GITHUB_SHA} .

      - name: Canary deploy (ejemplo)
        run: |
          echo "TODO: ejecutar script canary_deploy.sh"
          # ./scripts/canary_deploy.sh

      - name: Validar canary (ejemplo)
        run: |
          echo "TODO: ejecutar script canary_validate.sh"
          # ./scripts/canary_validate.sh

      - name: Promover o hacer rollback (ejemplo)
        run: |
          echo "TODO: ejecutar script canary_promote_or_rollback.sh"
          # ./scripts/canary_promote_or_rollback.sh
